$NetBSD$

--- wand/Wand-config.in.orig	2016-05-21 12:14:23.000000000 +0000
+++ wand/Wand-config.in
@@ -38,19 +38,19 @@ while test $# -gt 0; do
       echo '@PACKAGE_VERSION@ Q@QUANTUM_DEPTH@ @MAGICK_HDRI@'
       ;;
     --cflags)
-      pkg-config --cflags MagickWand
+      pkg-config --cflags MagickWand-6.Q16
       ;;
     --cxxflags)
-      pkg-config --cflags MagickWand
+      pkg-config --cflags MagickWand-6.Q16
       ;;
     --cppflags)
-      pkg-config --cflags MagickWand
+      pkg-config --cflags MagickWand-6.Q16
       ;;
     --ldflags)
-      pkg-config --libs MagickWand
+      pkg-config --libs MagickWand-6.Q16
       ;;
     --libs)
-      pkg-config --libs MagickWand
+      pkg-config --libs MagickWand-6.Q16
       ;;
     *)
       echo "${usage}" 1>&2
