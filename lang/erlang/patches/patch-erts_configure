$NetBSD$

Ensure we use GCC -shared not -G.

--- erts/configure.orig	2015-12-17 20:11:20.000000000 +0000
+++ erts/configure
@@ -44981,7 +44981,7 @@ case $host_os in
 		DED_LD_FLAG_RUNTIME_LIBRARY_PATH=
 	;;
 	solaris2*|sysv4*)
-		DED_LDFLAGS="-G"
+		DED_LDFLAGS="-shared"
 		if test X${enable_m64_build} = Xyes; then
 			DED_LDFLAGS="-64 $DED_LDFLAGS"
 		fi
