$NetBSD$

Help linker find -lperconaserverclient for percona

--- configure.orig	2015-05-28 00:25:54.000000000 +0000
+++ configure
@@ -37370,7 +37370,7 @@ if test x"$pr_use_mysql" = xyes; then
   LDFLAGS="$LDFLAGS $ac_build_addl_libdirs"
 
     LIBS=`echo "$LIBS" | sed -e 's/-lsupp//g'`;
-  LIBS="$LIBS -lm -lmysqlclient -lz"
+  LIBS="$LIBS $(mysql_config --libs)"
 
 
   { echo "$as_me:$LINENO: checking for MySQL's make_scrambled_password" >&5
